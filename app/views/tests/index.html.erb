<h1>Test</h1>

<table>
    <tr>
      <th>Tests</th>
      <th>Questions count</th>
    </tr>
  <% @tests.each do |test| %>
    <tr>
        <td><%= test.title %></td>
        <td><%= test.questions.count %></td>
      <td><%=button_to 'Start', start_test_path(test) %></td>
  <% end %>
    </tr>
</table>
